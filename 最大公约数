#include<stdio.h>

int main()
{
	int t, T, a, b;
	scanf("%d",&T);
	for(t=0;t<T;t++)
	{
		scanf("%d%d",&a,&b);
		if(a>b) 
		{
		int x = 0;
		x = a;
		a = b;
		b = x;
	    }
		for(int i=a;i>0;i--)
		{
			if((a/i)*i==a&&(b/i)*i==b)
			{
				printf("%d\n",i);
				break;
			}
		}
	}
	
	return 0;
} 
